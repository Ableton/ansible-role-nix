---
# Directory to use for nix
# Note: nix requires the installation directory to be installed at /nix, so if this fact
# is something other than /nix, then this role creates a symlink to that path at /nix.
nix_directory: "/nix"

# User to install nix for
nix_user: "{{ ansible_user }}"
